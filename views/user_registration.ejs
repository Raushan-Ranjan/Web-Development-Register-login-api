<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <style>
      body {
        background-color: rgba(223, 153, 24, 0.788);
        overflow: auto;
      }
      * {
        box-sizing: border-box;
      }

      .container {
        background-color: lightgrey;
        border-radius: 3px;
        padding: 10px 0px;
      }

      input,
      .btn {
        width: 100%;
        text-decoration: none;
        border: none;
        border-radius: 4px;
        padding: 20px;
        line-height: 20px;
        margin: 10px 0px;
        display: inline-block;
      }

      .col {
        float: left;
        width: 50%;
        margin: auto;
        padding: 0px 50px;
        margin-top: 6px;
        position: absolute;
        bottom: 3%;
        left: 30%;
      }

      .col {
        width: 40%;
        margin-top: 0;
        text-align: center;
      }

      .already {
        text-decoration: none;
        padding: 5px;
        margin: 10px;
        cursor: pointer;
      }

      .submit {
        background-color: green;
      }
    </style>
  </head>
  <body>
    <div class="container>">
      <div class="col">
        <div class="head">
          <img src="/images/Login.png" alt="Login Logo" />
          <h2>Register as USER</h2>
        </div>
        <form action="/user/registration" target="_self" method="POST">
          <input
            type="text"
            name="mobile"
            placeholder="Enter Mobile Number to Register "
            required
          />
          <input
            type="password"
            name="password"
            placeholder="Enter Password"
            required
          />

          <!-- <a href="/user/login" class="twitter btn">
            Register
          </a> -->
          <input type="submit" class="submit" value="Register" />

          <a href="/user/login" target="_self" class="already"
            >Already a USER ? LOGIN</a
          >
        </form>
      </div>
    </div>
  </body>
</html>
